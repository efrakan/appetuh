return {
  name         = "ccc",
  version      = "1.0.0",
  description  = "Lua source compiler/packer (bundle, minify, encrypt, lock).",
  dependencies = {},
  -- map form: install path -> source path on the repo server
  files        = { ["bin/ccc.lua"] = "/ccc.lua" },
}
